export abstract class loja {
 private _numeroId: number;
 private _produto: string;
 private _preco: number;
 private _tipo: number;
 private _estoque: number;
 private _quantProdutos: number = 0;
  
 constructor(numeroId: number, produto: string, preco: number, tipo: number, estoque: number,quantProdutos: number = 0) {
  this._numeroId = numeroId;
  this._produto = produto;
  this._preco = preco;
  this._tipo = tipo;
  this._estoque = estoque;
    }
   public get numeroId(): number { 
    return this._numeroId;
  }
    public set numeroId(numeroId: number) { 
        this._numeroId = numeroId;
    }   
   public get produto(): string {
        return this._produto;
    }
    public set produto(produto: string) {
        this._produto = produto;
    }
public get preco(): number {
        return this._preco;
    }
    public set preco(preco: number) {
        this._preco = preco;
    }

public get tipo(): number {
        return this._tipo;
    }
    public set tipo(tipo: number) {
        this._tipo =tipo;
    }

public get estoque(): number {
        return this._estoque;
    }
    public set estoque(estoque: number) {
        this._estoque = estoque;
    }
public get quantProdutos(): number {
        return this._quantProdutos;
    }
    public set quantProdutos(quantProdutos: number) {
        this._quantProdutos = quantProdutos;
    }


 public RetirarProduto(produto: string): boolean {

        if (this._estoque < produto.length) {
            console.log("\n Estoque Insuficiente!");
            return false;
        } else {
        this._estoque = this._quantProdutos + this._estoque; // Atualiza o estoque
            console.log("\n Produto retirado com sucesso!");
        return true;
        }
    }

    public AddProduto(produto: string): void {
        this._produto = this._produto + this._preco;
    }




public verificarfaturamento(): number {
    return this._quantProdutos * this._preco;
}


  public visualizar(): void {

        let tipo: string = "";

        switch (this._tipo) {
            case 1:
                tipo = "unha";
                break;
            case 2:
                tipo = "cabelo";
                break;
        }

        console.log("\n\n*****************************************************");
        console.log("Dados do produto:");
        console.log("*****************************************************");
        console.log("quantidade do  produto: " + this._quantProdutos);
        console.log("estoque: " + this._estoque);
        console.log("Tipo de produto: " + this._tipo);
        console.log("preco: " + this._preco.toFixed(2));
        console.log("produto: " + this._produto);
        console.log("numeroId: " + this._numeroId);

    }

}

























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































